<?php
/**
 * @file
 * Bean Panels Context main module file
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function bean_panels_context_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && $plugin == 'content_types') {
    return "plugins/content_types";
  }
}
